{
  "name": "marker-system",
  "version": "1.0.0",
  "description": "College Marks Entry System",
  "scripts": {
    "dev": "npm run dev --prefix src/web",
    "build": "npm run build --prefix src/web",
    "start": "npm run start --prefix src/web"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "tailwindcss": "^4.2.2"
  },
  "dependencies": {
    "next": "^16.2.2"
  }
}
